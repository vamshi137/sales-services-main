# Backend API Configuration
# CRITICAL: Must be the FULL API base URL including /api path
# Used by Vite at build time - ensure this is set in Vercel environment variables
# Note: All API requests automatically include ?i=1 to bypass InfinityFree anti-bot protection
VITE_API_BASE_URL=https://hrms1.free.nf/api
